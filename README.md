# taste-music-app
 TASTE — Music SPA (Vite + JSON-Server)

Jednostronicowa aplikacja muzyczna z trzema widokami: **Home**, **Search**, **Discover**.  
Dane ładowane z `JSON-Server`, odtwarzacze oparte o **GreenAudioPlayer**.

## Jak uruchomić lokalnie
```bash
npm install
npm run start
